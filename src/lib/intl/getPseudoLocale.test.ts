// import getPseudoLocale from './getPseudoLocale'

// describe('getPseudoLocale', () => {
//   const locale = 'pt-BR'
//   const defaultLocale = 'en-US'

//   test('Parse URL', () => {
//     expect(getPseudoLocale(locale, defaultLocale)).toEqual('pt')
//   })

//   test('Return defaultLocale', () => {
//     expect(getPseudoLocale('', defaultLocale)).toEqual('en')
//   })
// })
